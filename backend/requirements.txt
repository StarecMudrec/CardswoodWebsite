# ASGI app
quart[default]
hypercorn

# DB: async
sqlalchemy[asyncio]>=2.0
asyncpg
aiosqlite

# DB: sync (migrations only)
psycopg2-binary

# Auth / utils
joserfc
python-dotenv
uuid

# HTTP client (async)
httpx

# Legacy (migrations script still uses Flask)
flask
flask_migrate
flask_sqlalchemy

aiogram>=3.0,<4.0